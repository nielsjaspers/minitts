import { Output } from "../core/types";

export async function mergeWav(output: Output): Promise<void> {
    // TODO: implement
    // read all .wav files from output.dir
    // merge them into a single file
    // save the merged file to output.dir/output.wav
}
